<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.logistics">

  <application
    android:name="org.litepal.LitePalApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".ShipPositionActivity"></activity>
    <activity android:name=".NeedPlaceActivity" />
    <activity android:name=".Need_iActivity" />
    <activity android:name=".Store_iActivity" />
    <activity android:name=".SettingActivity" />
    <activity android:name=".ShipActivity" />
    <activity android:name=".StoreActivity" />
    <activity android:name=".FactoryActivity" />
    <activity android:name=".NeedActivity" />
    <activity android:name=".ItemActivity" />
    <activity android:name=".ItemList" />
    <activity android:name=".ListActivity" />
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>